services:
  wordpress:
    image: wordpress:latest
    container_name: ${PROJECT_NAME:-dirrigl-partner}-wordpress
    restart: unless-stopped
    environment:
      WORDPRESS_DB_HOST: db:3306
      WORDPRESS_DB_NAME: ${WORDPRESS_DB_NAME:-wordpress}
      WORDPRESS_DB_USER: ${WORDPRESS_DB_USER:-wordpress}
      WORDPRESS_DB_PASSWORD: ${WORDPRESS_DB_PASSWORD:-wordpress}
      WORDPRESS_DEBUG: ${WORDPRESS_DEBUG:-true}
      WORDPRESS_TABLE_PREFIX: ${WORDPRESS_TABLE_PREFIX:-TEyQzlMM}
      WORDPRESS_CONFIG_EXTRA: |
        define('WP_DEBUG', ${WORDPRESS_DEBUG:-true});
        define('WP_DEBUG_LOG', ${WORDPRESS_DEBUG:-true});
        define('WP_DEBUG_DISPLAY', false);
        define('SCRIPT_DEBUG', ${WORDPRESS_DEBUG:-true});
        define('WP_HOME', '${WP_URL:-http://dirrigl-partner.localhost}');
        define('WP_SITEURL', '${WP_URL:-http://dirrigl-partner.localhost}');
        define('WPMS_ON', true);
        define('WPMS_SMTP_HOST', 'mailpit');
        define('WPMS_SMTP_PORT', 1025);
        define('WPMS_SMTP_AUTH', false);
        define('WPMS_SSL', '');
        define('WPMS_MAILER', 'smtp');
    volumes:
      - wordpress_data:/var/www/html
      - ./wordpress/plugins:/var/www/html/wp-content/plugins
      - ./wordpress/mu-plugins:/var/www/html/wp-content/mu-plugins
      - ./wordpress/themes/dirrigl:/var/www/html/wp-content/themes/dirrigl
      - ./wordpress/uploads:/var/www/html/wp-content/uploads
    depends_on:
      db:
        condition: service_healthy
    networks:
      - internal
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.${PROJECT_NAME:-dirrigl-partner}.rule=Host(`${PROJECT_DOMAIN:-dirrigl-partner.localhost}`)"
      - "traefik.http.routers.${PROJECT_NAME:-dirrigl-partner}.entrypoints=web"
      - "traefik.http.services.${PROJECT_NAME:-dirrigl-partner}.loadbalancer.server.port=80"
      - "traefik.docker.network=proxy"

  wpcli:
    image: wordpress:cli
    container_name: ${PROJECT_NAME:-dirrigl-partner}-wpcli
    user: "33:33"
    volumes:
      - wordpress_data:/var/www/html
      - ./wordpress/plugins:/var/www/html/wp-content/plugins
      - ./wordpress/mu-plugins:/var/www/html/wp-content/mu-plugins
      - ./wordpress/themes/dirrigl:/var/www/html/wp-content/themes/dirrigl
      - ./wordpress/uploads:/var/www/html/wp-content/uploads
      - ./db-dumps:/db-dumps
    environment:
      WORDPRESS_DB_HOST: db:3306
      WORDPRESS_DB_NAME: ${WORDPRESS_DB_NAME:-wordpress}
      WORDPRESS_DB_USER: ${WORDPRESS_DB_USER:-wordpress}
      WORDPRESS_DB_PASSWORD: ${WORDPRESS_DB_PASSWORD:-wordpress}
      WORDPRESS_TABLE_PREFIX: ${WORDPRESS_TABLE_PREFIX:-TEyQzlMM}
    depends_on:
      db:
        condition: service_healthy
      wordpress:
        condition: service_started
    networks:
      - internal
    command: tail -f /dev/null

  db:
    image: mysql:8.0
    container_name: ${PROJECT_NAME:-dirrigl-partner}-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-rootpassword}
      MYSQL_DATABASE: ${WORDPRESS_DB_NAME:-wordpress}
      MYSQL_USER: ${WORDPRESS_DB_USER:-wordpress}
      MYSQL_PASSWORD: ${WORDPRESS_DB_PASSWORD:-wordpress}
    volumes:
      - db_data:/var/lib/mysql
      - ./db-dumps:/db-dumps
    networks:
      - internal
    healthcheck:
      test: [ "CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD:-rootpassword}" ]
      interval: 10s
      timeout: 5s
      retries: 5

  phpmyadmin:
    image: phpmyadmin:latest
    container_name: ${PROJECT_NAME:-dirrigl-partner}-phpmyadmin
    restart: unless-stopped
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-rootpassword}
    depends_on:
      - db
    networks:
      - internal
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.${PROJECT_NAME:-dirrigl-partner}-pma.rule=Host(`pma.${PROJECT_DOMAIN:-dirrigl-partner.localhost}`)"
      - "traefik.http.routers.${PROJECT_NAME:-dirrigl-partner}-pma.entrypoints=web"
      - "traefik.http.services.${PROJECT_NAME:-dirrigl-partner}-pma.loadbalancer.server.port=80"
      - "traefik.docker.network=proxy"

  mailpit:
    image: axllent/mailpit:latest
    container_name: ${PROJECT_NAME:-dirrigl-partner}-mailpit
    restart: unless-stopped
    networks:
      - internal
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.${PROJECT_NAME:-dirrigl-partner}-mail.rule=Host(`mail.${PROJECT_DOMAIN:-dirrigl-partner.localhost}`)"
      - "traefik.http.routers.${PROJECT_NAME:-dirrigl-partner}-mail.entrypoints=web"
      - "traefik.http.services.${PROJECT_NAME:-dirrigl-partner}-mail.loadbalancer.server.port=8025"
      - "traefik.docker.network=proxy"

volumes:
  wordpress_data:
  db_data:

networks:
  internal:
    driver: bridge
  proxy:
    external: true
